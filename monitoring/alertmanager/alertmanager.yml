route:
  receiver: alert-receiver
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: alert-receiver
    webhook_configs:
      - url: "http://alert-receiver:9100/alert"
        send_resolved: true
